import { useCallback, useMemo, useState } from "react";
import { Button, Chip, Dialog, DialogContent, DialogTitle } from "@mui/material";
import { ArrowLeft, Award, Building2, Eye, Gift, History, Medal, RefreshCw, Trophy, UsersRound } from "lucide-react";
import { useNavigate } from "react-router-dom";
import CustomizedTable from "../../../components/Table/CustomizedTable";
import { fetchRewardLeaderboard, fetchRewardMemberProfile } from "../../../services/rewardService";
import { createScopedClassNames } from "../../../utils/createScopedClassNames";
import StickySearchBar from "../StickySearchBar/StickySearchBar";
import styles from "./RewardMembersPage.module.css";

const cx = createScopedClassNames(styles);
const fmt = (value) => Number(value || 0).toLocaleString("en-IN");
const date = (value) => value ? new Date(value).toLocaleDateString("en-IN", { day: "2-digit", month: "short", year: "numeric" }) : "—";

export default function RewardMembersPage() {
  const navigate = useNavigate();
  const [members, setMembers] = useState([]); const [total, setTotal] = useState(0); const [loading, setLoading] = useState(false); const [detail, setDetail] = useState(null); const [detailLoading, setDetailLoading] = useState(false); const [refreshKey, setRefreshKey] = useState(0);
  const load = useCallback(async (page, limit) => { setLoading(true); try { const result = await fetchRewardLeaderboard({ page, limit }); setMembers(result.data || []); setTotal(result.total || 0); } finally { setLoading(false); } }, []);
  const open = async (member) => { setDetail({ member, profile: null }); setDetailLoading(true); try { setDetail({ member, profile: await fetchRewardMemberProfile(member.memberKey) }); } catch { setDetail({ member, profile: { error: "Reward activity could not be loaded." } }); } finally { setDetailLoading(false); } };
  const columns = useMemo(() => [
    { id: "rank", label: "Rank", renderCell: (_, row) => <span className={cx("rank")}>{row.rank <= 3 ? <Medal size={18} /> : `#${row.rank}`}</span> },
    { id: "displayName", label: "Member", renderCell: (_, row) => <div className={cx("member")}><i>{row.displayName.slice(0, 1).toUpperCase()}</i><div><b>{row.displayName}</b><span>MassClick rewards member</span></div></div> },
    { id: "tier", label: "Membership", renderCell: (_, row) => <Chip size="small" label={`${row.tier} member`} className={cx("tier", `tier-${String(row.tier).toLowerCase()}`)} /> },
    { id: "availablePoints", label: "Available points", renderCell: (_, row) => <strong className={cx("available")}>{fmt(row.availablePoints)} <small>pts</small></strong> },
    { id: "lifetimeEarned", label: "Lifetime earned", renderCell: (_, row) => <strong>{fmt(row.lifetimeEarned)} pts</strong> },
    { id: "view", label: "Details", renderCell: (_, row) => <Button className={cx("view")} variant="outlined" size="small" startIcon={<Eye size={16} />} onClick={() => open(row)}>View</Button> },
  ], []);
  const profile = detail?.profile;
  return <><StickySearchBar /><main className={cx("page")}><header><div><span><Trophy size={16} /> COMMUNITY REWARDS</span><h1>All user points</h1><p>A transparent view of reward balances and verified point activity across MassClick members.</p></div><div className={cx("head-actions")}><Button startIcon={<ArrowLeft size={17} />} onClick={() => navigate("/claim-rewards")}>Back to claims</Button><Button variant="contained" startIcon={<RefreshCw size={17} />} onClick={() => setRefreshKey((value) => value + 1)}>Refresh</Button></div></header><section className={cx("summary")}><div><UsersRound /><span><small>Reward members</small><strong>{fmt(total)}</strong></span></div><div><Award /><span><small>Verified point records</small><strong>Live directory</strong></span></div><div><Gift /><span><small>Privacy protected</small><strong>No contact data shown</strong></span></div></section><section className={cx("table")}><CustomizedTable title="Member points directory" columns={columns} data={members} total={total} fetchData={load} loading={loading} enableStatusFilter={false} searchPlaceholder="Search members" refreshKey={refreshKey} /></section></main><Dialog open={Boolean(detail)} onClose={() => setDetail(null)} fullWidth maxWidth="md" PaperProps={{ className: cx("dialog") }}><DialogTitle><div><span><Award size={15} /> MEMBER REWARD PROFILE</span><h2>{detail?.member?.displayName}</h2><p>Points, completed claims and reward activity.</p></div><Button onClick={() => setDetail(null)}>Close</Button></DialogTitle><DialogContent>{detailLoading ? <div className={cx("loading")}>Loading reward activity…</div> : profile?.error ? <div className={cx("loading")}>{profile.error}</div> : <><div className={cx("wallet")}><div><small>Available points</small><strong>{fmt(profile?.wallet?.availablePoints)} pts</strong></div><div><small>Lifetime earned</small><strong>{fmt(profile?.wallet?.lifetimeEarned)} pts</strong></div><div><small>Redeemed</small><strong>{fmt(profile?.wallet?.lifetimeRedeemed)} pts</strong></div><Chip label={`${profile?.wallet?.tier || "Silver"} member`} /></div><section><h3><Building2 size={18} /> Businesses & claims</h3>{profile?.claims?.length ? <div className={cx("activity")}>{profile.claims.map((claim) => <article key={claim._id}><div><b>{claim.businessName}</b><span>{claim.categoryName} · {claim.locationName || "Location not provided"}</span><small>{date(claim.transactionAt)} · ₹{fmt(claim.transactionAmount)}</small></div><strong>{fmt(claim.awardedPoints || claim.projectedPoints)} pts <em>{claim.status}</em></strong></article>)}</div> : <p className={cx("empty")}>No verified purchase claims yet.</p>}</section><section><h3><History size={18} /> Points timeline</h3>{profile?.transactions?.length ? <div className={cx("activity")}>{profile.transactions.map((item) => <article key={item._id}><div><b>{item.description || item.milestone.replaceAll("_", " ")}</b><span>{date(item.createdAt)}</span></div><strong className={item.status === "debited" ? cx("debit") : ""}>{item.status === "debited" ? "−" : "+"}{fmt(item.points)} pts</strong></article>)}</div> : <p className={cx("empty")}>No point transactions yet.</p>}</section></>}</DialogContent></Dialog></>;
}
